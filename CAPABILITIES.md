{% raw %}
# CAR Capability Manifest

<!-- Generated by scripts/build-capabilities.sh. Do not edit by hand. -->

Schema v2. 14 groups claim 75 workspace crates and 23 named programs/binaries exactly once.

## Capability groups

### Runtime foundations (`runtime-foundations`)

Validated agent IR, deterministic DAG execution, state, portability, and one relocatable runtime root.

**Workspace crates**

| Crate | Description | README |
|---|---|---|
| `car-engine` | Core runtime engine for Common Agent Runtime | yes |
| `car-home` | Single state-root resolution (CAR_HOME) for Common Agent Runtime | yes |
| `car-ir` | Agent IR types for Common Agent Runtime | yes |
| `car-runtime` | Umbrella entry point for external Rust consumers of Common Agent Runtime | yes |
| `car-state` | State store for Common Agent Runtime | yes |
| `car-validator` | Precondition checking and action validation for Common Agent Runtime | yes |
| `car-winenv` | Windows cmd.exe environment repair (over-long PATH) for Common Agent Runtime | yes |

**Programs and binaries**

| Program | Kind | Entry point | What it provides |
|---|---|---|---|
| — | — | — | — |

### Verification and governance (`verification-governance`)

Static plan checks, policy enforcement, permission preflight, and sandboxed execution boundaries.

**Workspace crates**

| Crate | Description | README |
|---|---|---|
| `car-permissions` | Cross-platform permission preflight for Common Agent Runtime | yes |
| `car-policy` | Policy engine for Common Agent Runtime | yes |
| `car-sandbox` | Sandboxed execution surface for CAR — process isolation primitives for untrusted agent steps | yes |
| `car-verify` | Static plan verification for Agent IR | yes |

**Programs and binaries**

| Program | Kind | Entry point | What it provides |
|---|---|---|---|
| — | — | — | — |

### Memory and learning (`memory-learning`)

Graph memory, spreading-activation recall, learned skills, procedural structure, compaction, and outer-loop optimization.

**Workspace crates**

| Crate | Description | README |
|---|---|---|
| `car-harness-opt` | Outer-loop optimizer that searches over context-assembly harness configurations. | yes |
| `car-memgine` | Memgine — graph-based memory engine for Common Agent Runtime | yes |
| `car-procgraph` | Procedural graph core — admissible procedure transitions and step-level guidance for Common Agent Runtime | yes |

**Programs and binaries**

| Program | Kind | Entry point | What it provides |
|---|---|---|---|
| `car-harness-opt` | Binary | `crates/car-harness-opt/src/bin/main.rs` | Executable target from car-harness-opt: Outer-loop optimizer that searches over context-assembly harness configurations. |

### Inference and models (`inference-models`)

Local and remote model routing, typed conversations, ChatGPT-subscription inference through the Codex CLI that holds the login, proposal generation, and static proposal ranking.

**Workspace crates**

| Crate | Description | README |
|---|---|---|
| `car-active-planner` | Active planner for CAR — generates, scores, and selects proposals via inference | yes |
| `car-inference` | Local model inference for CAR — Candle backend with Qwen3 models | yes |
| `car-inference-types` | Pure-serde conversation/message wire types for Common Agent Runtime inference — shared by car-inference and car-sync so the transcript-resume projection can never drift from the real Message enum | yes |
| `car-planner` | Proposal scoring and search for Common Agent Runtime | yes |

**Programs and binaries**

| Program | Kind | Entry point | What it provides |
|---|---|---|---|
| `mlx_safetensors_repro` | Binary | `crates/car-inference/src/bin/mlx_safetensors_repro.rs` | Executable target from car-inference: Local model inference for CAR — Candle backend with Qwen3 models |

### Agents and orchestration (`agent-orchestration`)

Supervised, declarative, external, peer, commodity, and multi-agent execution with guarded coordination.

**Workspace crates**

| Crate | Description | README |
|---|---|---|
| `car-agents` | Built-in commodity agents for Common Agent Runtime | yes |
| `car-bundle` | Manifest format, canonicalization, and signing for CAR contributed-agent bundles. | no |
| `car-external-agents` | Detection of installed agentic CLIs (Claude Code, Codex, Gemini) for the Common Agent Runtime. | yes |
| `car-fleet` | Composite inventory of the CAR agents, capabilities and models reachable across a fleet of instances, and the wire contract for farming verified coding subtasks out to them. | yes |
| `car-multi` | Multi-agent coordination patterns for Common Agent Runtime | yes |
| `car-peers` | Agent-to-agent message envelope, addressing, and delivery guards for Common Agent Runtime. | yes |
| `car-registry` | File-based agent registry + lifecycle supervisor for Common Agent Runtime. | yes |
| `car-topology` | Amortized coordination-topology selection core for Common Agent Runtime | yes |

**Programs and binaries**

| Program | Kind | Entry point | What it provides |
|---|---|---|---|
| `car do` | Program | `car-cli/src/do_cmd.rs + car-server-core/src/assistant` | Flagship sandbox-first assistant with tools, graph memory, policies, verified goals, and receipt-grounded completion. |
| `commodity agents` | Program | `car-agents/src/lib.rs` | Reusable researcher, planner, verifier, summarizer, and coordinator roles for multi-agent compositions. |
| `declagents` | Program | `car-registry/src/declarative.rs` | Pure-data in-daemon agents with tool allowlists, standing goals, routing, and no child process. |
| `foreman` | Program | `car-multi/src/patterns/foreman + car-server-core/src/coder/foreman_loop.rs` | Parallel coding orchestration with footprint scheduling and fail-closed merge verification. |
| `newsroom` | Program | `external consumer over runs.* + car-cli/src/main.rs (car ui)` | Durable newsroom run capture, transcript resume, cancellation, and operator viewing over the run-trace surface. |
| `registry / supervisor` | Program | `car-registry/src/lib.rs + supervisor.rs` | Observed-agent registration and declarative process lifecycle with restart, health, and captured logs. |

### Coding automation (`coding-automation`)

Code reasoning, AST-aware change analysis, external reference search, and verified coding-task construction.

**Workspace crates**

| Crate | Description | README |
|---|---|---|
| `car-ast` | Tree-sitter AST parsing for code-aware inference | yes |
| `car-builder` | Natural-language → validated car-workflow manifest builder for Common Agent Runtime | yes |
| `car-reason` | Code reasoning engine for Common Agent Runtime — adaptive, graph-driven, learning | yes |
| `car-search` | External code discovery + indexing for Common Agent Runtime | yes |

**Programs and binaries**

| Program | Kind | Entry point | What it provides |
|---|---|---|---|
| `coder` | Program | `car-cli/src/code_task.rs + car-server-core/src/coder` | Interactive and headless coding runs with outcome contracts, isolated worktrees, and a verified merge gate. |

### Workflows and scheduling (`workflows-scheduling`)

Validated stage graphs, saga compensation, durable schedules, and natural-language workflow authoring.

**Workspace crates**

| Crate | Description | README |
|---|---|---|
| `car-scheduler` | Task scheduling and background execution for Common Agent Runtime | yes |
| `car-workflow` | Declarative multi-stage workflow orchestration for Common Agent Runtime | yes |

**Programs and binaries**

| Program | Kind | Entry point | What it provides |
|---|---|---|---|
| `scheduler` | Program | `car-scheduler/src/lib.rs` | Persistent once, interval, file-watch, cron, and manual tasks with native OS schedule rendering. |
| `workflows / builder` | Program | `car-workflow + car-builder` | Conditional compensable workflows plus bounded natural-language generation and static validation. |

### Tools and integrations (`tools-integrations`)

Remote MCP tools, account-backed productivity data, Parslee services, and native account discovery.

**Workspace crates**

| Crate | Description | README |
|---|---|---|
| `car-accounts` | OS-native account discovery for Common Agent Runtime | yes |
| `car-connectors` | Remote MCP connectors for the Common Agent Runtime — connect to remote MCP servers over HTTP, register their tools, and route calls through CAR's governance layer (validator, policy, eventlog). | yes |
| `car-integrations` | OS-native account-bound integrations (Calendar, Contacts, Mail) for CAR | yes |
| `car-parslee` | Parslee platform integration for the CAR daemon — OAuth session (auth), entitlements/capability discovery, M365 docs/search, and the parslee.* tool surface. Extracted from car-server-core (#418 Phase 2). | yes |

**Programs and binaries**

| Program | Kind | Entry point | What it provides |
|---|---|---|---|
| — | — | — | — |

### Voice, media, and perception (`voice-media-perception`)

Speech, meetings, browser and desktop control, vision, language, translation, health, and OS automation.

**Workspace crates**

| Crate | Description | README |
|---|---|---|
| `car-automation` | macOS automation bridges (AppleScript, Shortcuts, App Intents) for Common Agent Runtime | yes |
| `car-browser` | Browser automation and perception pipeline for Common Agent Runtime | yes |
| `car-desktop` | OS-level screen capture, accessibility inspection, and input synthesis for Common Agent Runtime | yes |
| `car-health` | Cross-platform wearable / activity data for Common Agent Runtime | yes |
| `car-meeting` | Meeting capture and persistence on top of car-voice — multi-source sessions, .car/meetings/ on-disk format, segment ingest | yes |
| `car-nlp` | Natural-language primitives for CAR — language id, tokenization, named-entity recognition, sentiment. Apple NaturalLanguage framework on macOS; pure-Rust fallback (whatlang + Unicode UAX #29) on other platforms. | yes |
| `car-soundanalysis` | Audio-event classification for CAR via Apple's SoundAnalysis framework. Identifies speech, music, sound events from audio files using on-device classifiers. | yes |
| `car-translate` | Text translation for CAR via Apple's Translation framework on macOS 14+. Stub today — needs a Swift shim because no objc2-translation crate exists. | yes |
| `car-vision` | Vision (OCR, layout, classification) for Common Agent Runtime — Apple Vision on macOS, native Windows.Media.Ocr on Windows, Tesseract fallback elsewhere | yes |
| `car-voice` | Voice I/O capability for CAR — mic capture, VAD, listener/speaker traits | yes |
| `car-whisper` | In-process whisper.cpp speech-to-text (model resolve/download + transcription) for Common Agent Runtime — a leaf crate shared by car-voice and car-inference | yes |

**Programs and binaries**

| Program | Kind | Entry point | What it provides |
|---|---|---|---|
| — | — | — | — |

### Protocols and connectivity (`protocols-connectivity`)

WebSocket protocol types, MCP and A2A interoperability, A2UI surfaces, sync, leases, and daemon clients.

**Workspace crates**

| Crate | Description | README |
|---|---|---|
| `car-a2a` | Bridge between Common Agent Runtime and the Linux Foundation Agent2Agent (A2A) v1.0 protocol | yes |
| `car-a2ui` | A2UI protocol model and surface store for CAR | no |
| `car-daemon-client` | WebSocket JSON-RPC client to the singleton CAR daemon — session.auth handshake, id-routed request/response multiplexing, server-initiated tools.execute callbacks, and the shared auth-token IO. The transport core every FFI binding (NAPI, PyO3, UniFFI) proxies through. | yes |
| `car-mcp` | MCP (Model Context Protocol) server library — transport-agnostic dispatch for exposing CAR capabilities. Used by car-mcp-server (stdio binary) and car-server (HTTP-streamable daemon endpoint). | yes |
| `car-mcp-server` | MCP (Model Context Protocol) server binary — stdio transport for the car-mcp library. For HTTP-streamable, embed car-mcp directly into a daemon (see car-server-core). | yes |
| `car-proto` | JSON-RPC protocol types for Common Agent Runtime client-server communication | yes |
| `car-sync` | Multi-device sync core for Common Agent Runtime — replica-tagged append-only oplog + deterministic CRDT fold | yes |

**Programs and binaries**

| Program | Kind | Entry point | What it provides |
|---|---|---|---|
| `car-mcp` | Binary | `crates/car-mcp-server/src/main.rs` | Executable target from car-mcp-server: MCP (Model Context Protocol) server binary — stdio transport for the car-mcp library. For HTTP-streamable, embed car-mcp directly into a daemon (see car-server-core). |

### Host and daemon platform (`host-platform`)

The daemon, terminal host, native CLI, shared host state, and the agent-driven UI improvement loop.

**Workspace crates**

| Crate | Description | README |
|---|---|---|
| `car-cli` | Native CLI for Common Agent Runtime | yes |
| `car-feedback-core` | Diagnostic bundle collection and redaction for CAR's in-app feedback system — daemon-free leaf crate | yes |
| `car-host` | Terminal host CLI for Common Agent Runtime | yes |
| `car-server` | CAR daemon — WebSocket server for the Common Agent Runtime | yes |
| `car-server-core` | Transport-neutral library for the CAR daemon JSON-RPC dispatcher (used by car-server and tokhn-daemon) | yes |
| `car-server-types` | Shared host-state + channel/approval types for the CAR daemon, lifted out of car-server-core so messaging/parslee/coder can extract without a dependency cycle (#418) | yes |
| `car-ui-agent` | In-process UI-improvement agent: consumes a2ui.render_report telemetry from the renderer and emits patchComponents envelopes back via memgine-backed rendering strategies | yes |

**Programs and binaries**

| Program | Kind | Entry point | What it provides |
|---|---|---|---|
| `car` | Binary | `crates/car-cli/src/main.rs` | Executable target from car-cli: Native CLI for Common Agent Runtime |
| `car-host` | Binary | `crates/car-host/src/main.rs` | Executable target from car-host: Terminal host CLI for Common Agent Runtime |
| `car-server` | Binary | `crates/car-server/src/main.rs` | Executable target from car-server: CAR daemon — WebSocket server for the Common Agent Runtime |

### Identity, auth, secrets, and messaging (`identity-auth-secrets`)

OAuth identity, native secret storage, assistant naming, and governed human approval transports.

**Workspace crates**

| Crate | Description | README |
|---|---|---|
| `car-auth` | Shared Parslee OAuth2 PKCE + token/keychain logic for the CAR CLI and daemon | yes |
| `car-identity` | The flagship assistant's user-chosen name, spoken aliases, and vocative matching | yes |
| `car-messaging` | Multi-channel messaging (iMessage + Slack) for the CAR daemon — approval transports (inbound poller/orchestrator, Slack wire parsing, per-channel config/allowlist/pairing) AND the general outbound send surface backing the runtime's messaging.send tool. Extracted from car-server-core (#418) to cut its test-binary link footprint. | yes |
| `car-secrets` | Cross-platform secret store for Common Agent Runtime | yes |

**Programs and binaries**

| Program | Kind | Entry point | What it provides |
|---|---|---|---|
| — | — | — | — |

### Observability and evaluation (`observability-evaluation`)

Tamper-evident events, telemetry, conformance, benchmarks, and StateBench memory evaluation.

**Workspace crates**

| Crate | Description | README |
|---|---|---|
| `car-bench` | Benchmark harness for CAR — proves runtime value via frontier model benchmarks | yes |
| `car-conformance` | Conformance test runner — verifies Rust runtime matches Python spec | yes |
| `car-eventlog` | Event log with JSONL persistence for Common Agent Runtime | yes |
| `car-memgine-eval` | StateBench evaluation bridge for Rust car-memgine | yes |
| `car-selfheal` | Deterministic self-healing signal detection core for Common Agent Runtime | yes |
| `car-telemetry` | Tracing and OpenTelemetry initialization for CAR | yes |

**Programs and binaries**

| Program | Kind | Entry point | What it provides |
|---|---|---|---|
| `backend-compare` | Binary | `crates/car-bench/src/backend_compare.rs` | Executable target from car-bench: Benchmark harness for CAR — proves runtime value via frontier model benchmarks |
| `car-bench` | Binary | `crates/car-bench/src/main.rs` | Executable target from car-bench: Benchmark harness for CAR — proves runtime value via frontier model benchmarks |
| `car-bench-harness` | Binary | `crates/car-bench/src/harness_bench_cli.rs` | Executable target from car-bench: Benchmark harness for CAR — proves runtime value via frontier model benchmarks |
| `car-bench-run` | Binary | `crates/car-bench/src/cli_run.rs` | Executable target from car-bench: Benchmark harness for CAR — proves runtime value via frontier model benchmarks |
| `car-foreman-eval` | Binary | `crates/car-bench/src/foreman_eval_cli.rs` | Executable target from car-bench: Benchmark harness for CAR — proves runtime value via frontier model benchmarks |
| `car-memgine-eval` | Binary | `crates/car-memgine-eval/src/main.rs` | Executable target from car-memgine-eval: StateBench evaluation bridge for Rust car-memgine |
| `car-self-harness` | Binary | `crates/car-bench/src/self_harness_cli.rs` | Executable target from car-bench: Benchmark harness for CAR — proves runtime value via frontier model benchmarks |

### Developer bindings (`developer-bindings`)

Node.js, Python, Swift, and Kotlin access to CAR with shared binding logic and generated native bridges.

**Workspace crates**

| Crate | Description | README |
|---|---|---|
| `car-ffi-common` | Shared logic for FFI bindings (NAPI, PyO3) — JSON wrappers for verify, multi-agent, scheduler | yes |
| `car-ffi-napi` | Node.js/TypeScript bindings for Common Agent Runtime via napi-rs | yes |
| `car-ffi-pyo3` | Python bindings for Common Agent Runtime via PyO3 | yes |
| `car-ffi-uniffi` | UniFFI bindings for the Common Agent Runtime — generates Swift / Kotlin bindings from a single .udl. Primary consumer is the macOS / iOS host app, which declares App Intents that invoke CAR via this crate. | yes |

**Programs and binaries**

| Program | Kind | Entry point | What it provides |
|---|---|---|---|
| `uniffi-bindgen` | Binary | `crates/car-ffi-uniffi/src/bin/uniffi-bindgen.rs` | Executable target from car-ffi-uniffi: UniFFI bindings for the Common Agent Runtime — generates Swift / Kotlin bindings from a single .udl. Primary consumer is the macOS / iOS host app, which declares App Intents that invoke CAR via this crate. |

## CLI command tree

Hidden clap commands are excluded.

| Command | Description |
|---|---|
| `car accounts` | OS-native account discovery — Internet Accounts / WAM / GOA |
| `car accounts list` | List accounts visible to the OS |
| `car accounts open` | Open the native account-management UI |
| `car agent` | Build and run in-daemon agents from plain-language descriptions. An agent runs inside CAR — nothing to install |
| `car agent external` | Show installed external agentic CLIs (Claude Code, Codex, Gemini): which binary each resolved to, and whether it can actually run |
| `car agent list` | List registered in-daemon agents |
| `car agent new` | Describe an agent in plain language; CAR builds, verifies, and registers it to run in-daemon |
| `car agent run` | Run a registered agent on an input |
| `car agent where` | Show where a declarative or supervised agent's files live |
| `car approvals` | Agent approval policy — the default posture for what agents may do without asking. Proxies the daemon's `agent_permissions.*` surface (the CLI counterpart to the CarHost "Approvals" settings / onboarding step) |
| `car approvals default` | Set the default approval preset for all agents (`cautious` asks before any edit/full-access, `balanced` allows sandboxed edits, `trusting` allows everything without asking) |
| `car approvals evaluate-tool` | Show whether one exact agent/tool override is active and schema-bound |
| `car approvals get` | Show the current default approval posture |
| `car approvals reset-tool` | Remove one exact agent/tool approval override |
| `car approvals set-tool` | Set an approval mode for one exact agent and exact tool. `always_allow` requires that agent to be attached with an eligible live callback |
| `car auth` | Parslee account authentication for CAR cloud-backed features |
| `car auth accounts` | List all stored Parslee logins (active marked) |
| `car auth login` | Sign in with a Parslee account using browser-based OAuth + PKCE |
| `car auth logout` | Remove stored Parslee account tokens from the OS keychain |
| `car auth orgs` | List the organizations the signed-in account belongs to (active marked) |
| `car auth status` | Show whether CAR has a usable Parslee account token |
| `car auth switch-account` | Switch the active Parslee login by account id |
| `car auth switch-org` | Switch the account's active organization by id (e.g. `org_parslee`) |
| `car board` | Fullscreen supervision board for coder sessions — one screen for every run on this host, whoever started it (`car code`, CarHost, milo, another board). Attach to a run's full history, confirm its outcome contract, answer its questions, approve its diff, or scope work in a repo-grounded discussion first. Closing the board never stops a run |
| `car bookmarks` | Browser bookmark integration |
| `car bookmarks list` | List Safari bookmarks |
| `car browse` | Browser automation — drive Chromium through the CAR runtime |
| `car browse run` | Execute a script of browser operations in order. See docs for the script JSON schema (see crates/car-cli/BROWSE.md once published) |
| `car browse schema` | Print the script schema and the list of supported operations |
| `car build` | Build a runnable workflow from a natural-language goal |
| `car cal` | OS-native Calendar integration |
| `car cal events` | List events in an ISO-8601 time range |
| `car cal list` | List visible calendars across all sources |
| `car capabilities` | Print the deterministic CAR capability manifest |
| `car code` | Built-in coding agent: state an intent, confirm the verifiable outcome contract, and CAR delivers it in an isolated git worktree — natively or via an installed frontier CLI. Results land on a `car/coder/<id>` branch after your approval; your checkout is never touched |
| `car code-task` | Run a coder session headlessly and IN THIS PROCESS: derive or accept an outcome contract, work in a git worktree until the runtime's own re-run of that contract is green, then deliver the result as a pull request |
| `car coder-ab` | A/B-test CAR's coder against an external agent (Codex / Claude Code) over a corpus, and grow that corpus from git history — the productionized dogfooding loop (docs/proposals/coder-ab-dogfood.md) |
| `car coder-ab extract` | Grow the corpus from a git repo's history: for each recent commit that fixes code AND touches a test, materialize the pre-fix (failing) state as a task (parent's source + the commit's test), keep only tasks whose test actually fails at that state, and append them to a corpus. Best-effort candidate generation for human review |
| `car coder-ab run` | Run the paired A/B over a corpus manifest, verify each arm against the task's own contract, and write a JSON report. Exits non-zero when CAR is significantly *behind* the external arm (McNemar), so it doubles as a CI regression gate. Needs a running daemon (`car code` routes to it) |
| `car contacts` | OS-native Contacts integration |
| `car contacts containers` | List containers (a.k.a. sources / accounts within Contacts) |
| `car contacts find` | Free-text contact search |
| `car daemon` | Start the daemon server (delegates to car-server binary) |
| `car do` | CAR Assistant: a general-purpose agent that works out of the box |
| `car doctor` | Diagnose (and optionally repair) a CAR install: corrupt model weights, unparseable `~/.car` state files, version skew, and leftover files from a previous install. Runs entirely against the local filesystem — no daemon required, so it works even when `car-server` won't start |
| `car dream` | Run memory consolidation ("dream") — prune, GC, embed, evolve |
| `car embed` | Generate embeddings for text |
| `car eval` | Evaluate agent improvement from stored trajectories |
| `car feedback` | Report a problem to Parslee. Captures a redacted diagnostic bundle (your description, the `car doctor` report, bounded log tails, and a version stamp) into the local outbox at `~/.car/feedback-outbox` — nothing is uploaded by this command; queued reports send when CAR can reach Parslee. Works with the daemon down, like `car doctor`. macOS-only in this release |
| `car files` | Account-backed file locations |
| `car files locations` | List standard account-backed file locations |
| `car fleet` | What every CAR instance you can reach can do — agents, capabilities and models across the fleet — and whether this machine takes coding subtasks farmed out by peers (`car fleet enroll`). Needs a running daemon |
| `car fleet enroll` | Enroll this machine as a fleet worker for the named repositories |
| `car fleet run` | Run one coding goal across the fleet: decompose it, place the independent subtasks on the instances that have this repository, and gate the reassembly **here** |
| `car fleet show` | Every agent, capability, and model across this daemon and every CAR instance it can reach |
| `car fleet withdraw` | Stop taking work from peers. Keeps the repository list for next time |
| `car fleet worker` | Show whether this machine takes coding subtasks farmed out by peers |
| `car heal` | Inspect and operate the daemon's self-healing REPAIR loop: it reads a configured issue tracker, runs a coder session, gates the result on a multi-model panel, and opens a pull request. It never merges |
| `car heal run` | Run one sweep now instead of waiting for the cadence |
| `car heal status` | Show whether the loop is enabled — and if not, why — plus its targets, review panel, and engine |
| `car health` | Wearable / activity data (HealthKit + Fitbit/Garmin/Oura/etc.) |
| `car health activity` | Daily activity summaries across a date range (YYYY-MM-DD) |
| `car health sleep` | Sleep windows in an ISO-8601 time range |
| `car health status` | Backend availability + guidance |
| `car health workouts` | Workouts in an ISO-8601 time range |
| `car identity` | Show or change the name your assistant answers to — in conversation, in the host apps, and as its voice wake word |
| `car identity set` | Name the assistant. Takes effect for its system prompt, the host apps, and its voice wake word |
| `car identity set-user` | Tell the assistant what to call YOU |
| `car identity show` | Show the current name, the spoken forms it wakes on, and where the record lives |
| `car image` | Generate an image with a local MLX model |
| `car infer` | Run text generation with a local model |
| `car info` | Show runtime and live machine state |
| `car init` | Initialize a .car/ project directory for team-shared configuration |
| `car inspect` | Inspect a single installed agent (Parslee-ai/car#182 phase 4). Shows the on-disk manifest plus the supervisor's current status. Resolves unqualified names to the highest installed semver per the contributed-agents proposal |
| `car install` | Install a contributed agent — from a local directory OR a registry reference (Parslee-ai/car#182 phases 4 + 5) |
| `car keychain` | OS keychain integration |
| `car keychain status` | Backend availability status |
| `car keys` | Store cloud-provider API keys in the OS keychain, so a native-app user never sets an environment variable (docs/proposals/native-secrets-no-env.md). The key is read env-first, keychain-fallback by the runtime |
| `car keys list` | Show which provider keys are configured (keychain or env); values hidden |
| `car keys remove` | Remove a stored provider key from the keychain |
| `car keys set` | Store a provider key in the keychain. `<name>` is a provider (openai/anthropic/gemini/elevenlabs/github/aws) or a raw env-var name. Omit `<value>` to read it from stdin (keeps it out of shell history) |
| `car ls` | List every installed contributed agent (Parslee-ai/car#182 phase 4). Calls the daemon's `agents.list` and renders the result. Long alias: `agents` |
| `car mail` | OS-native Mail integration |
| `car mail accounts` | List mail accounts known to the backend |
| `car mail body` | Fetch one message body by the `id` from `car mail messages` |
| `car mail inbox` | Inbox summaries per account |
| `car mail mailboxes` | List every mailbox (folder) per account, nested ones included (on Microsoft Graph the nested walk is bounded: depth 8, 64 requests) |
| `car mail messages` | Read message rows from one mailbox, newest first across all matched accounts (not newest-first per account, then concatenated) |
| `car mail send` | Send (or draft) a message. JSON payload on stdin matching SendRequest |
| `car messages` | OS-native Messages integration |
| `car messages chats` | List recent Messages.app chats |
| `car messages read` | Read Messages.app conversation rows, newest first |
| `car messages send` | Send a message. JSON payload on stdin matching Messages SendRequest |
| `car messages services` | List Messages.app services/accounts |
| `car models` | Manage local inference models |
| `car models add` | Add any HuggingFace model by repo id, deriving its schema automatically |
| `car models adopt` | Adopt an already-usable local artifact into CAR ownership |
| `car models benchmark` | Benchmark curated models and write benchmark priors for cold-start routing |
| `car models discover` | Discover models exposed by a running vLLM-MLX server |
| `car models doctor` | Report configured defaults, provider health, and modality coverage |
| `car models list` | List available models and download status |
| `car models preflight` | Evaluate one local model without downloading or loading it |
| `car models pull` | Download a model |
| `car models recommend` | Recommend the best model for this machine and what you want to do |
| `car models register` | Register a custom model from a JSON schema file |
| `car models remove` | Remove a downloaded model |
| `car models resource-policy` | Inspect or update the local-model RAM policy |
| `car models resource-policy get` | Show the saved policy and its evaluated machine budget |
| `car models resource-policy set` | Persist a policy. Custom values are exact MB in 512 MB increments |
| `car models route` | Route a prompt and show which model would be selected (dry run) |
| `car models serve` | Start an external MLX runtime for a cataloged local-server model |
| `car models smoke` | Run representative live model checks across text, code, tool, vision, and speech |
| `car models stats` | Show performance stats for models based on observed outcomes |
| `car models unregister` | Unregister a model by ID |
| `car models upgrade` | Upgrade installed models to curated newer replacements when CAR can do so |
| `car models upgrades` | Show installed models that have curated newer replacements |
| `car notes` | macOS Notes integration |
| `car notes accounts` | List Notes.app accounts |
| `car notes find` | Free-text note search |
| `car onboard` | Onboarding readiness — what's set up and what's still pending (the headless equivalent of the CarHost setup wizard) |
| `car optimize` | Optimize a proposal (remove phantom dependencies) |
| `car parslee` | Parslee platform capabilities for the signed-in account (requires `car auth login`) |
| `car parslee capabilities` | Discover what your Parslee account can do — identity, product entitlements, and Studio reachability. Read-only |
| `car parslee generate-document` | Generate a Word document from a natural-language brief and save it to your connected drive (OneDrive/Google Drive). Requires the `aie` product |
| `car permissions` | OS permission preflight — TCC / Windows Privacy / xdg-desktop-portal |
| `car permissions domains` | List all known domains |
| `car permissions explain` | Human-readable explanation and fix suggestion |
| `car permissions request` | Trigger a native prompt if the OS supports one |
| `car permissions status` | Report current grant state for a domain |
| `car photos` | macOS Photos integration |
| `car photos albums` | List Photos.app albums |
| `car policy-check-hook` | Evaluate a host's proposed tool call against `.car/policies/` and answer on stdout with that host's hook decision envelope |
| `car project` | Manage CAR-managed projects (a named, git-backed workspace CAR creates for you — no repo to pick) |
| `car project list` | List your managed projects |
| `car project new` | Create a managed project (idempotent — re-running loads it) |
| `car publish` | Publish a contributed agent to the registry (Parslee-ai/car#182 phase 5). Reads a local agent's `manifest.toml`, signs it for `--audience public` (ed25519 key at `$CAR_PUBLISH_KEY_PATH`), stages it at the versioned `agents/<namespace>/<name>/<version>/` path, updates `index.json` + the README catalog, re-runs the EXACT `car registry validate` CI gate locally, and opens a PR against the registry repo. Aborts (no PR) on a missing signing key or a validation failure |
| `car purge` | Remove this CAR install's own state under `~/.car` (config, logs, managed models, binaries) and reap any OS-level schedules CAR installed — for a clean slate before a reinstall. On macOS this also clears CarHost.app's user-level state and resets its privacy (TCC) permissions, so a reinstall really does re-run permission onboarding. NEVER touches the shared HuggingFace model cache (other tools use it); managed models are symlinks into it, so only the links are removed, not the multi-GB blobs. (To uninstall a single contributed agent instead, use `car uninstall <id>`.) |
| `car reason` | Reason about code: diagnose, suggest fixes, explain |
| `car registry` | Registry tooling for the contributed-agents registry (Parslee-ai/car#182 phase 5): `digest` a manifest, or `validate` a registry directory (the CI gate run by Parslee-ai/car-agent-registry) |
| `car registry digest` | Print the canonical SHA-256 hex digest of a manifest.toml |
| `car registry schema` | Print the canonical registry index JSON Schema (the single source of truth, embedded in this binary) to stdout. Regenerate the registry repo's copy with: `car registry schema > schema/index.schema.json` |
| `car registry validate` | Validate a registry directory (index.json + manifests + schema). This is the registry CI gate |
| `car reminders` | macOS Reminders integration |
| `car reminders items` | List incomplete reminders |
| `car reminders lists` | List Reminders.app lists |
| `car replay` | Replay an event journal and show reconstructed state |
| `car restart` | Restart an installed contributed agent |
| `car run-cancel` | Cancel one active CAR run and print its deterministic durable receipt |
| `car run-task` | Run a goal autonomously against stdio MCP tool servers, emitting a JSONL transcript. Headless entry point for external eval harnesses |
| `car schedule` | Schedule commands to run on a cadence (launchd / cron / schtasks) |
| `car schedule add` | Schedule a command to run on a cadence |
| `car schedule list` | List scheduled commands and whether each is OS-durable |
| `car schedule reconcile` | Reconcile OS schedules against the task store, reaping orphans |
| `car schedule remove` | Remove a scheduled command: drops any OS schedule and deletes the task |
| `car secrets` | OS-native secret store — Keychain / Credential Manager / Secret Service. Aliased as `secret` (singular) for backward compatibility |
| `car secrets available` | Probe whether the OS secret store is reachable on this host |
| `car secrets delete` | Delete a secret (idempotent) |
| `car secrets get` | Retrieve a secret — prints the value on stdout |
| `car secrets migrate-from-env` | Walk known remote-model env vars and copy any non-empty values from process env into the OS keychain. Idempotent — running again only rewrites entries when the env value differs from what's stored |
| `car secrets put` | Store a secret. Value is read from --value or piped stdin |
| `car secrets status` | Check whether a secret exists, without returning the value |
| `car selfheal` | Inspect and operate the daemon's deterministic self-healing detector |
| `car selfheal dismiss` | Dismiss one active detection by dedup key |
| `car selfheal fix` | Start one bounded coder round for an eligible recurring-tool key |
| `car selfheal list` | List active detections, optionally filtered by kind, severity, or time |
| `car selfheal run` | Run one deterministic detection tick and its auto-fix cadence hook now |
| `car selfheal show` | Print the trusted local issue document for one detection |
| `car selfheal status` | Show cadence, active counts, and the route resolved by the last tick |
| `car setup` | Set up the right model for this machine — detect hardware, recommend, and install. Run with no flags for an interactive walkthrough |
| `car simulate` | Simulate a proposal's state effects without executing |
| `car skills` | Manage learned skills (SkillRL-inspired) |
| `car skills distill` | Distill skills from an event log journal |
| `car skills list` | List skills in a memory file |
| `car speech` | Manage CAR speech runtime, health checks, and smoke tests |
| `car speech doctor` | Report managed speech runtime, local model cache, and remote-provider health |
| `car speech install` | Install the managed speech runtime and curated local speech models |
| `car speech smoke` | Run end-to-end speech smoke tests through CAR |
| `car start` | Start an installed contributed agent |
| `car stop` | Stop an installed contributed agent |
| `car tail-log` | Tail the most recent stdout/stderr lines captured from a supervised agent (Parslee-ai/car#231 §5.2). Calls the daemon's `agents.tail_log`. Closes the DX gap where the daemon captured logs to `~/.car/logs/` (`%USERPROFILE%\.car\logs\` on Windows) but offered no CLI surface to read them. Long alias: `logs` |
| `car tools` | Invoke CAR's built-in runtime tools directly, in-process and without a daemon |
| `car tools call` | Invoke one CAR built-in tool in this process; no daemon is contacted |
| `car ui` | Open the browser dashboard served by the daemon |
| `car uninstall` | Uninstall a contributed agent (Parslee-ai/car#182 phase 4). Stops the running child if any, removes the manifest from `~/.car/agents/<id>/`, and reaps the legacy `agents.json` entry. Idempotent |
| `car update` | Update the locally-installed `car` CLI and its sibling `car-server` daemon to the latest release (or `--version <X.Y.Z>`), in place — regardless of how they were installed. Reconciles the drift that otherwise builds up when one channel updates and another doesn't (e.g. CarHost.app auto-updates its bundled daemon via Sparkle but leaves the `/usr/local/bin/car` CLI behind). The npm/PyPI `car-runtime` client packages are separate — this command never touches them; `car doctor` reports which of your agents have drifted, and prints the exact command per environment. Both remedies pin: `npm install car-runtime@<version>` (`npm update` CANNOT cross a 0.x minor — npm reads `^0.41.0` as `>=0.41.0 <0.42.0`, so it is a no-op) and `<venv>/bin/python -m pip install -U car-runtime==<version>` (a bare `-U` can be silently defeated by the consumer's own pin, and the wrong interpreter installs into an environment that does not hold the stale wheel) |
| `car verify` | Statically verify a proposal |
| `car video` | Generate a video with a local MLX model |
| `car voice` | Voiceprint enrollment — teach CAR your voice so it knows who's speaking (the owner drives commands; other voices are context). Proxies the daemon's `voice.*` surface |
| `car voice enroll` | Enroll a voiceprint from a WAV recording. The daemon decodes the file and saves a voiceprint under `~/.car/voiceprints/<label>.toml` |
| `car voice list` | List enrolled voiceprints |
| `car voice remove` | Remove an enrolled voiceprint by label |

## Model-facing tool registries

### Assistant

| Tool | Description |
|---|---|
| `browse_click` | Click on a UI element by accessibility node ID |
| `browse_keypress` | Press a key with optional modifiers |
| `browse_navigate` | Navigate the browser to a URL |
| `browse_observe` | Observe the current browser state: take screenshot, extract accessibility tree, produce UiMap |
| `browse_scroll` | Scroll the browser page |
| `browse_type` | Type text into a UI element by accessibility node ID |
| `browse_wait` | Wait for a browser condition to be met |
| `browser_await_answer` | After you submit a question or trigger an action in a web app, call this to WAIT until the response has finished rendering, before you screenshot or stop a recording. It polls the page and returns once the content stops changing. Use it every time between submitting and observing/recording an answer — browse_observe does NOT wait, so without this you capture the page mid-load (a blank or still-thinking state) instead of the actual answer. |
| `browser_await_signin` | Ask the USER to sign in, on whatever surface this browser has, and wait until they have. Use this the moment a site needs authentication — you cannot and must not type someone's credentials, but the person can complete any flow (SSO, MFA, a device prompt) themselves: in the CAR app's browser drawer when the app is running, or in the browser window if one is open. TELL THE USER what to sign into before calling this, and say to look in the CAR app if they do not see a browser window; it blocks while they do it. The session persists in the browser profile, so this is a one-time cost per site rather than per run. |
| `browser_record_start` | Start RECORDING the browser session to video. Pair it with the browse_* tools: start recording, drive the app (navigate, type a real question, wait for the answer), then call browser_record_stop to get an MP4. Use it whenever the ASK is a product demo, an onboarding or training clip, a bug repro, or release notes — anything where showing the app BEING USED beats a screenshot of its final state. Frames are captured only when the page actually CHANGES, so make sure something happens on screen; a static page records nothing. |
| `browser_record_stop` | Stop the recording started by browser_record_start and write an MP4 under the working directory. Returns the path plus the real duration. Requires ffmpeg. |
| `calculate` | Evaluate a mathematical expression exactly. Prefer this over arithmetic in your head or a shell one-liner whenever a number has to be right. |
| `classify_image` | Identify what an image depicts — returns ranked content labels with confidence (e.g. "golden retriever", "airliner", "beach"). Give a path to an image under the working directory. Use this to check what a generated or downloaded image actually shows. |
| `delegate` | Hand one self-contained sub-task to a fresh sub-agent that shares your model, permissions, and working directory but starts with an EMPTY transcript: it sees only the goal you write, not this conversation. Use it to keep a long exploration or a noisy batch of tool output out of your own context. It runs to completion before this call returns and you receive ONLY its final written answer, so put everything it needs in `goal` and ask it to report exactly what you need back. It cannot delegate further. |
| `edit_file` | Make a targeted edit to an existing file by replacing `old_text` with `new_text`. Prefer this over write_file for changing part of a file — it never risks clobbering the rest. `old_text` and `new_text` must be the EXACT raw file text: do NOT include the line-number prefixes that read_file displays (the `%6d\t` before each line are display-only — copy only the text after the tab). By default `old_text` must match EXACTLY ONE place in the file; if it matches several, add surrounding lines to make it unique, or set `replace_all: true` to replace every occurrence. You must read the file (read_file) earlier in this session before editing it, and re-read it if it changed on disk since — the runtime rejects an edit to a file you have not read (or that is stale). A paged read is sufficient only for one unique targeted replacement; replace_all requires a fresh unpaged read. |
| `events_query` | Query this run's event log: what you already tried, what failed, and what the runtime did. Use it before retrying an approach that may have already failed, and after a history compaction notice to recover what was removed from the transcript. Returns bounded summaries, most-recent first — not full payloads. |
| `find_files` | Find files by name or glob pattern within a directory tree. A bare pattern (no '/') matches file names at any depth (e.g. `*.rs` finds every Rust file); a pattern with '/' matches the path relative to the search root, and `**` spans directories (e.g. `src/**/*.rs`). |
| `generate_image` | Generate an image from a text prompt using CAR's local image model, write it as a PNG under the working directory, and return its path. Use this to create real visual assets — illustrations, art, backgrounds, icons, textures, logos — that you can then reference from HTML/CSS or place in a project. The result is a file path; read or embed it like any other file. Generation takes roughly a minute, so prefer a few well-chosen images over many. |
| `generate_jingle` | Generate a short sonic-branding jingle (branded audio) for a brand or product via Parslee Studio. Writes an audio file under the working directory and returns its path — use it for brand stings, app/game intros, ad audio, logo sounds. Takes ~30s. The result is a file path; embed it or read it like any file. |
| `generate_music` | Generate an original music track from a text prompt via Parslee Studio (ElevenLabs Music). Writes an audio file under the working directory and returns its path — use it for game soundtracks, background music, intros, ambience. Takes ~30s. The result is a file path; embed it (e.g. <audio src>) or read it like any file. |
| `generate_song` | Generate a full SONG WITH VOCALS AND LYRICS via Parslee Studio (Suno), up to 8 minutes — distinct from generate_music, which makes shorter instrumental / ambience tracks. Use it when you want an actual song: a theme with a sung chorus, a branded anthem, or lyrics you supply set to music. Writes an audio file under the working directory and returns its path. Takes a few minutes (async). The result is a file path; embed it or read it like any file. |
| `generate_speech` | Generate spoken audio from text using CAR's local voice model, write it as a WAV under the working directory, and return its path. Use this for narration, voiceover, a spoken UI, or game/video audio — real synthesized speech, not a text placeholder. The result is a file path; embed it (e.g. <audio controls src="...">) or read it like any other file. |
| `generate_studio_image` | Generate a HIGH-QUALITY image via Parslee Studio (gpt-image-2). Unlike the local generate_image, this reliably renders LEGIBLE TEXT inside the image (titles, signage, logos with words) and follows complex prompts more faithfully — use it for posters, covers, UI mockups with real labels, or any image that must contain readable text. Writes the image under the working directory and returns its path. It is SLOW (often 1–4 minutes) and near its time budget, so it can occasionally return a timeout error — if that happens, retry once, or fall back to the local generate_image (fast) when you don't need readable in-image text. The result is a file path; reference it from HTML/CSS or read it like any file. |
| `generate_video` | Animate a STILL IMAGE into a short video clip (image-to-video), or generate a clip from text alone, via Parslee Studio. Give it `image_path` to animate an existing image — camera moves, drifting light, subtle motion — or omit it for text-to-video. Writes an MP4 under the working directory and returns its path. Takes roughly 1–4 minutes per clip. IMPORTANT: this is a diffusion model that repaints every frame, so any fine TEXT, NUMBERS, TABLES, CHARTS, or UI in the source image WILL be warped into gibberish. Use it on pictorial, abstract, or title imagery. To add motion to a text-heavy slide or screenshot, do NOT use this — keep the image crisp and animate it with an ffmpeg pan/zoom (Ken Burns) via the shell instead. |
| `generate_voiceover` | Generate NARRATION / VOICEOVER speech from text in a SPECIFIC voice via Parslee Studio (ElevenLabs), including this organization's cloned real-person voices. Writes an audio file under the working directory and returns its path plus its measured duration in seconds. Prefer this over generate_speech whenever the voice matters — generate_speech uses a local stock voice and CANNOT do a named or cloned voice. Use it for video narration, explainers, training courses, audiobooks, and per-slide voiceover. Synchronous, a few seconds per call. Call list_voices first to choose a voice. |
| `grep_files` | Search file contents recursively with a regex, returning each match as `{path, line, text}`. `line` is 1-based and matches the line numbers read_file shows, so grep to locate code and then read_file/edit_file that exact spot. Only text source files are scanned (binary and oversized files are skipped) and dotfiles/`node_modules`/`__pycache__`/`target` are ignored. Use `max_results` to bound output (default 50). Use find_files instead to locate files by NAME rather than content. |
| `http_request` | Fetch a URL or call an HTTP API. Defaults to GET. Returns the response status and a (size-capped) body. Runs from the host, so it works even when the filesystem/shell are sandboxed offline. |
| `linked_devices` | List the user's linked CAR host devices and their advertised consumer capabilities, such as iPhone chat, approvals, notifications, and push-to-talk. Read-only; does not access contacts, location, photos, microphone, or other private phone data. |
| `list_dir` | List the immediate entries of a directory (NON-recursive), returning `{name, path, is_dir, size_bytes}` for each. Hidden dotfiles and `node_modules`/`__pycache__`/`target` are omitted. For a recursive or globbed search use find_files; to search file contents use grep_files. |
| `list_voices` | List the speaking voices available for generate_voiceover — both Studio's stock presets and any voices this organization has CLONED from real recordings of real people. Call this FIRST whenever the user asks for narration 'in my voice', in a named person's voice, or in a particular style, so you can pick the right one instead of guessing. Read-only and instant. Each entry has an id, a name, and a source ('cloned' = a real person's voice, 'preset' = a stock voice). |
| `m365_task` | Delegate a task to the user's Microsoft 365 assistant (their Parslee AI Employee) — email, calendar, contacts, CRM (HubSpot), meetings, and business research across their connected M365 and tools. Give a plain-language instruction like 'summarize my unread email from today', 'what's on my calendar tomorrow', 'draft a follow-up to the Acme thread', or 'find our latest deal with Contoso in HubSpot'. Returns the assistant's reply (and a conversation_id you can pass back to continue the same thread). IMPORTANT: actions that SEND or CHANGE things (send an email, create a calendar event) are drafted and returned for approval, not executed silently — relay the reply and let the user confirm. Mailbox / calendar access needs a connected Microsoft account; if it isn't connected the reply says so. |
| `notify_linked_device` | Send a short CAR notification to a linked consumer device that advertises notifications.deliver, such as the user's iPhone. This only delivers title/body notification text through the CAR host event channel; it does not access contacts, location, photos, microphone, files, or arbitrary phone actions. |
| `produce_commercial` | Produce a short COMMERCIAL VIDEO (shots, voiceover, and music) from a creative brief via Parslee Studio's video pipeline. Give it a brief describing the product and the ad you want; Studio plans shots, generates keyframes and video, adds a voiceover and a music bed, and assembles a finished MP4. Writes the video under the working directory and returns its path. This is SLOW — a real production runs many minutes (up to ~25). Use it when the user wants an actual video ad, promo, or commercial (for music/jingles/images use the other Studio tools). The result is a file path; embed it (<video src>) or read it like any file. |
| `read_file` | Read a UTF-8 text file and return its contents. The returned `content` is LINE-NUMBERED in `cat -n` style: every line is prefixed with its 1-based line number, right-aligned in 6 columns, then a tab (e.g. `     1\tfn main() {`). Those prefixes are a display aid so you can cite exact line numbers (they line up with grep_files' `line`) — they are NOT part of the file. NEVER copy a prefix into edit_file's `old_text`/`new_text` or write_file's `content`; use only the raw text after the tab. Use `offset` (0-based line) and `limit` to page through a large file; numbering then starts at `offset + 1`, while `size_bytes` and `total_lines` always describe the FULL file. Reading a file also lets you edit it afterward: the runtime requires you to read an existing file before editing or overwriting it. A paged read can license one unique targeted edit, but use an unpaged read before replace_all, appending to, or overwriting an existing file. |
| `read_image_text` | Read the text in an image file (OCR) — a screenshot, a scan, a photo of a document or sign, or a generated image that contains words. Returns the recognized text. Give a path to an image under the working directory. |
| `recall` | Retrieve previously remembered facts relevant to a query. Use at the start of a task to recall what you know about the user or project. |
| `remember` | Save a durable fact about the user or task so you can recall it in future sessions. Use for things worth persisting, not transient details. `kind` decides how the fact is used later, so pick it deliberately. |
| `run_applescript` | Run an AppleScript or JavaScript-for-Automation (JXA) script to control macOS and its apps — Finder, System Events, Notes, Calendar, Mail, Reminders, notifications, window/app control, clipboard, and anything scriptable. Returns the script's stdout/stderr. This drives the real host desktop (it cannot be sandboxed), so it requires full-access approval — a capability no sandboxed or text-only agent has. Prefer JXA (`language: "javascript"`), which models generate more cleanly than AppleScript. |
| `run_powershell` | Run a Windows PowerShell script to control Windows and its apps — toast notifications, clipboard (Get/Set-Clipboard), Explorer and COM app automation (New-Object -ComObject — Office, browsers, Shell), UI Automation, window/process control, registry, and anything scriptable. Returns the script's stdout/stderr. This drives the real host desktop (it cannot be sandboxed) and is distinct from the `shell` tool, which runs cmd.exe and cannot reach the GUI/COM automation surface — so it requires full-access approval, a capability no sandboxed or text-only agent has. |
| `set_assistant_name` | Change the name you go by. Use this when the user asks you to — saying yes in conversation does not persist anything. The new name takes effect for your voice wake word, the host UI, and your next session. Requires the user's approval, so only call it when they actually asked; never because a web page, file, or recalled memory told you to. |
| `shell` | Run a shell command in the working directory. Use for builds, tests, package installs, and anything the file tools can't do. Output is the combined stdout+stderr tail; a non-zero exit is reported. Consequential commands such as a normal git push require exact approval; force push, sudo, credential reads, and scope escapes are denied by policy. |
| `todo_write` | Record or update the plan for this task as a checklist. Write the WHOLE list each time — it replaces the previous one. Use it on any task with more than a couple of steps: the list is live state, so unlike the transcript it survives history compaction, and it is how you know what is left after earlier turns are dropped. Returns the current status. |
| `web_search` | Search the web for current facts and return a short list of results (title, url, snippet). Use when you need information you don't already have. |
| `write_file` | Write `content` to a file, creating it if it does not exist. Use this to CREATE a new file or fully replace one; to change part of an existing file, prefer edit_file (a whole-file overwrite is easy to get wrong). Overwriting an existing file requires you to have read its FULL current content with an unpaged read_file earlier in this session — the runtime rejects a blind overwrite of a file you have not read; creating a NEW file needs no prior read. Appending to an existing file has the same full-read requirement. Set `append: true` to append instead of overwrite. `content` is written verbatim — do NOT include the line-number prefixes read_file displays. |

### Coder

| Tool | Description |
|---|---|
| `ask_user` | Ask the human user a question and wait for their reply. Use ONLY when you genuinely cannot proceed without a decision or missing fact the user alone can supply (an ambiguous requirement, a destructive choice, a missing credential). Do not use it for things you can determine by reading the repo or running commands. The call blocks until the user answers or a timeout elapses; on timeout you receive an error and should proceed with your best judgment. |
| `browse_click` | Click on a UI element by accessibility node ID |
| `browse_keypress` | Press a key with optional modifiers |
| `browse_navigate` | Navigate the browser to a URL |
| `browse_observe` | Observe the current browser state: take screenshot, extract accessibility tree, produce UiMap |
| `browse_scroll` | Scroll the browser page |
| `browse_type` | Type text into a UI element by accessibility node ID |
| `browse_wait` | Wait for a browser condition to be met |
| `browser_await_answer` | After you submit a question or trigger an action in a web app, call this to WAIT until the response has finished rendering, before you screenshot or stop a recording. It polls the page and returns once the content stops changing. Use it every time between submitting and observing/recording an answer — browse_observe does NOT wait, so without this you capture the page mid-load (a blank or still-thinking state) instead of the actual answer. |
| `browser_await_signin` | Ask the USER to sign in, on whatever surface this browser has, and wait until they have. Use this the moment a site needs authentication — you cannot and must not type someone's credentials, but the person can complete any flow (SSO, MFA, a device prompt) themselves: in the CAR app's browser drawer when the app is running, or in the browser window if one is open. TELL THE USER what to sign into before calling this, and say to look in the CAR app if they do not see a browser window; it blocks while they do it. The session persists in the browser profile, so this is a one-time cost per site rather than per run. |
| `browser_record_start` | Start RECORDING the browser session to video. Pair it with the browse_* tools: start recording, drive the app (navigate, type a real question, wait for the answer), then call browser_record_stop to get an MP4. Use it whenever the ASK is a product demo, an onboarding or training clip, a bug repro, or release notes — anything where showing the app BEING USED beats a screenshot of its final state. Frames are captured only when the page actually CHANGES, so make sure something happens on screen; a static page records nothing. |
| `browser_record_stop` | Stop the recording started by browser_record_start and write an MP4 under the working directory. Returns the path plus the real duration. Requires ffmpeg. |
| `edit_file` | Make a targeted edit to an existing file by replacing `old_text` with `new_text`. Prefer this over write_file for changing part of a file — it never risks clobbering the rest. `old_text` and `new_text` must be the EXACT raw file text: do NOT include the line-number prefixes that read_file displays (the `%6d\t` before each line are display-only — copy only the text after the tab). By default `old_text` must match EXACTLY ONE place in the file; if it matches several, add surrounding lines to make it unique, or set `replace_all: true` to replace every occurrence. You must read the file (read_file) earlier in this session before editing it, and re-read it if it changed on disk since — the runtime rejects an edit to a file you have not read (or that is stale). A paged read is sufficient only for one unique targeted replacement; replace_all requires a fresh unpaged read. |
| `find_files` | Find files by name or glob pattern within a directory tree. A bare pattern (no '/') matches file names at any depth (e.g. `*.rs` finds every Rust file); a pattern with '/' matches the path relative to the search root, and `**` spans directories (e.g. `src/**/*.rs`). |
| `grep_files` | Search file contents recursively with a regex, returning each match as `{path, line, text}`. `line` is 1-based and matches the line numbers read_file shows, so grep to locate code and then read_file/edit_file that exact spot. Only text source files are scanned (binary and oversized files are skipped) and dotfiles/`node_modules`/`__pycache__`/`target` are ignored. Use `max_results` to bound output (default 50). Use find_files instead to locate files by NAME rather than content. |
| `list_dir` | List the immediate entries of a directory (NON-recursive), returning `{name, path, is_dir, size_bytes}` for each. Hidden dotfiles and `node_modules`/`__pycache__`/`target` are omitted. For a recursive or globbed search use find_files; to search file contents use grep_files. |
| `read_file` | Read a UTF-8 text file and return its contents. The returned `content` is LINE-NUMBERED in `cat -n` style: every line is prefixed with its 1-based line number, right-aligned in 6 columns, then a tab (e.g. `     1\tfn main() {`). Those prefixes are a display aid so you can cite exact line numbers (they line up with grep_files' `line`) — they are NOT part of the file. NEVER copy a prefix into edit_file's `old_text`/`new_text` or write_file's `content`; use only the raw text after the tab. Use `offset` (0-based line) and `limit` to page through a large file; numbering then starts at `offset + 1`, while `size_bytes` and `total_lines` always describe the FULL file. Reading a file also lets you edit it afterward: the runtime requires you to read an existing file before editing or overwriting it. A paged read can license one unique targeted edit, but use an unpaged read before replace_all, appending to, or overwriting an existing file. |
| `recall` | Retrieve previously remembered facts relevant to a query. Use at the start of a task to recall what you know about the user or project. |
| `report_no_change` | Conclude that NO code should change, and end the session by reporting that instead of producing a diff. Use this only when you have investigated and established one of: the reported problem does not exist (the code already handles it); the behaviour is intentional; or the real fix is not a code change at all. This is NOT a way to stop when the task is hard — a session that has already edited any file cannot use it, and reverting does not restore eligibility. Your conclusion is a nomination: the runtime checks it against the baseline results and may route it to a human rather than accept it. |
| `shell` | Run a shell command at the repository root (the worktree). Use for builds, tests, and anything the file tools can't do. Output is the combined stdout+stderr tail. Publishing and privilege-escalating commands are denied by policy: `git push`, `gh`/`glab` writes (`pr create`, `release create`, non-GET `api`), `npm`/`cargo publish`, `docker push`, `sudo`, and destructive operations outside the repo. Reading the forge is allowed (`gh pr view`, `gh run view`, `gh api` GET) so you can watch CI. Do not try to route around these — the runtime opens the pull request itself after it has verified your work. |
| `write_file` | Write `content` to a file, creating it if it does not exist. Use this to CREATE a new file or fully replace one; to change part of an existing file, prefer edit_file (a whole-file overwrite is easy to get wrong). Overwriting an existing file requires you to have read its FULL current content with an unpaged read_file earlier in this session — the runtime rejects a blind overwrite of a file you have not read; creating a NEW file needs no prior read. Appending to an existing file has the same full-read requirement. Set `append: true` to append instead of overwrite. `content` is written verbatim — do NOT include the line-number prefixes read_file displays. |

### MCP stdio

| Tool | Description |
|---|---|
| `equivalent` | Check whether two ActionProposals leave the same state behind. This SAMPLES: it probes the states in test_states and nothing else — two trivial defaults (empty, and {x:1, y:2}) when you pass none. A false is a witness, some sampled state separates the two proposals. A true means only that none of the sampled states did; it is not a claim that they never diverge, so widen test_states to raise your confidence. The result carries 'tier': 'sampled', 'states_tested' and 'used_default_states' so a client reads how the answer was derived — including whether the true came off the two trivial defaults or off states you chose — without parsing this text. |
| `memory_add_fact` | Ingest a fact into CAR's graph memory. Kind defaults to "pattern"; use "constraint" for hard rules. |
| `memory_delete` | Delete a proactive memory entry by fact id, or clear a private status id like proactive-status:global. |
| `memory_evaluate` | Evaluate proactive memory on labeled cases against selective, always-inject, passive-retrieval, and no-memory baselines. |
| `memory_intervene` | Select at most one targeted proactive memory reminder for the next action, or return an explicit silent decision. |
| `memory_query` | Query CAR graph memory using spreading activation. Returns top-k nodes with activation scores. |
| `memory_save_knowledge` | Save durable proactive knowledge such as task requirements, policies, verified environment facts, and constraints. |
| `memory_save_procedural` | Save durable proactive procedural evidence such as failed attempts, successful fixes, diagnostics, and tool gotchas. |
| `memory_update_status` | Update proactive memory's private progress/risk status. Status is session-local and not exposed through generic context retrieval. |
| `optimize` | Rewrite an ActionProposal to expose more parallelism: drop every state_dependency naming a key no action in the proposal writes, so the DAG builder can place more actions in one execution level. Action order and everything else are unchanged. This REWRITES, it does not check — a pruned dependency is one `verify` would have flagged as unavailable, so treat the returned proposal as a new input to `verify` rather than reading the rewrite as a repair. 'pruned' lists exactly what was dropped, per action. |
| `policy_check` | Evaluate a proposed tool call against CAR's policy layer BEFORE it runs. Built for a host's PreToolUse hook: pass the tool name and its parameters, get back allow/deny with the rule that decided it. Merges the operator's declarative rules from <CAR_HOME>/policies/ and .car/policies/ (under the working directory; neither is walked upward) with CAR's stateless egress guardrail. Read 'basis' as well as 'decision' — 'no_rules_configured' means nothing was loaded and the allow reviewed nothing, which is NOT the same as 'passed_rules'. An unparseable policy file denies ('policy_load_failed') rather than failing open. Findings carry 'source' so an operator-authored rule is distinguishable from a built-in guardrail, and 'severity' so a warn is distinguishable from a deny. |
| `simulate` | Predict the state an executor would leave behind after running an ActionProposal, by applying each action's DECLARED expected_effects. Runs no tools and has no side effects. An action whose preconditions or state dependencies are unsatisfied contributes nothing, and the actions downstream of it drop out with it, so the cascade follows the data dependencies. A declared effect is ASSUMED to land — this predicts what the declarations imply, not what a tool would really do — and failure_behavior is not modelled, so an independent action alongside a blocked one still contributes here even though the executor's default Abort may never reach it. Read the result as the state assuming execution proceeds as far as the dependency graph allows, never as a claim that a blocked action ran. |
| `skill_find` | Find top-k skills matching a persona/url/task triple, ranked by activation. |
| `skill_ingest` | Ingest a skill into CAR's graph memory. A skill is executable code associated with a trigger (persona + url pattern + task keywords) so skill_find can later retrieve it for matching tasks. |
| `skill_list` | Enumerate all ingested skills. Optional domain filter returns only skills scoped Global or Domain(domain). |
| `verify` | Statically verify an ActionProposal: detect dependency cycles, missing tools, and simulate final state. No execution, no side effects. Each issue carries a 'tier' — decision_procedure \| heuristic \| sampled — naming which kind of check produced it, so a rule of thumb (loop detection) is distinguishable from an exact check (tool registration) without reading the message text. |

## Daemon JSON-RPC dispatch methods

470 string-literal dispatch methods across 82 namespaces; 0 are not referenced by `docs/websocket-protocol.md`.

Namespaces are the prefix before the first `.` or `/`; unqualified and PascalCase aliases are grouped under `root`. Roles are freshly derived from the daemon handler's authorization call sites: `host` for host-management authority, `owner` for per-resource ownership, `agent` for a bound agent identity, and `operator` otherwise. They describe an auth-enabled deployment because host gates are conditional on a configured host token.

### `a2a`

| Method | Caller role | Documented |
|---|---|---|
| `a2a.peers.add` | `operator` | yes |
| `a2a.peers.list` | `operator` | yes |
| `a2a.peers.remove` | `operator` | yes |
| `a2a.send` | `operator` | yes |
| `a2a.start` | `operator` | yes |
| `a2a.status` | `operator` | yes |
| `a2a.stop` | `operator` | yes |

### `a2ui`

| Method | Caller role | Documented |
|---|---|---|
| `a2ui.action` | `operator` | yes |
| `a2ui.apply` | `operator` | yes |
| `a2ui.capabilities` | `operator` | yes |
| `a2ui.get` | `operator` | yes |
| `a2ui.ingest` | `operator` | yes |
| `a2ui.reap` | `operator` | yes |
| `a2ui.render_report` | `operator` | yes |
| `a2ui.surfaces` | `operator` | yes |
| `a2ui/replay` | `operator` | yes |
| `a2ui/subscribe` | `operator` | yes |
| `a2ui/unsubscribe` | `operator` | yes |

### `accounts`

| Method | Caller role | Documented |
|---|---|---|
| `accounts.list` | `operator` | yes |
| `accounts.open` | `operator` | yes |

### `admission`

| Method | Caller role | Documented |
|---|---|---|
| `admission.status` | `operator` | yes |

### `agent`

| Method | Caller role | Documented |
|---|---|---|
| `agent/getAuthenticatedExtendedCard` | `operator` | yes |

### `agent_permissions`

| Method | Caller role | Documented |
|---|---|---|
| `agent_permissions.evaluate` | `operator` | yes |
| `agent_permissions.evaluate_tool` | `host` | yes |
| `agent_permissions.get` | `operator` | yes |
| `agent_permissions.reset` | `host` | yes |
| `agent_permissions.reset_tool` | `host` | yes |
| `agent_permissions.set` | `host` | yes |
| `agent_permissions.set_default` | `host` | yes |
| `agent_permissions.set_tool` | `host` | yes |

### `agents`

| Method | Caller role | Documented |
|---|---|---|
| `agents.chat` | `agent` | yes |
| `agents.chat.approve` | `operator` | yes |
| `agents.chat.cancel` | `operator` | yes |
| `agents.detect_external` | `operator` | yes |
| `agents.health` | `operator` | yes |
| `agents.health_external` | `operator` | yes |
| `agents.install` | `host` | yes |
| `agents.invoke_external` | `operator` | yes |
| `agents.list` | `operator` | yes |
| `agents.list_external` | `operator` | yes |
| `agents.message` | `operator` | yes |
| `agents.message.approve` | `operator` | yes |
| `agents.message.pending` | `operator` | yes |
| `agents.peers` | `operator` | yes |
| `agents.remove` | `host` | yes |
| `agents.restart` | `agent` | yes |
| `agents.start` | `agent` | yes |
| `agents.stop` | `agent` | yes |
| `agents.tail_log` | `operator` | yes |
| `agents.upsert` | `host` | yes |
| `agents.wait` | `operator` | yes |

### `assistant`

| Method | Caller role | Documented |
|---|---|---|
| `assistant.identity.get` | `operator` | yes |
| `assistant.identity.set` | `host` | yes |

### `assistants`

| Method | Caller role | Documented |
|---|---|---|
| `assistants.invoke` | `operator` | yes |

### `auth`

| Method | Caller role | Documented |
|---|---|---|
| `auth.accounts` | `host` | yes |
| `auth.authority_hint` | `host` | yes |
| `auth.complete` | `host` | yes |
| `auth.completion_status` | `host` | yes |
| `auth.logout` | `host` | yes |
| `auth.remove_account` | `host` | yes |
| `auth.snapshot` | `host` | yes |
| `auth.start` | `host` | yes |
| `auth.status` | `host` | yes |
| `auth.switch_account` | `host` | yes |
| `auth.switch_org` | `host` | yes |

### `automation`

| Method | Caller role | Documented |
|---|---|---|
| `automation.run_applescript` | `operator` | yes |
| `automation.run_powershell` | `operator` | yes |
| `automation.shortcuts.list` | `operator` | yes |
| `automation.shortcuts.run` | `operator` | yes |

### `bookmarks`

| Method | Caller role | Documented |
|---|---|---|
| `bookmarks.list` | `operator` | yes |

### `browser`

| Method | Caller role | Documented |
|---|---|---|
| `browser.close` | `operator` | yes |
| `browser.producer.register` | `operator` | yes |
| `browser.run` | `operator` | yes |
| `browser.view.back` | `operator` | yes |
| `browser.view.click` | `operator` | yes |
| `browser.view.forward` | `operator` | yes |
| `browser.view.hand_back` | `operator` | yes |
| `browser.view.keypress` | `operator` | yes |
| `browser.view.navigate` | `operator` | yes |
| `browser.view.paste` | `operator` | yes |
| `browser.view.reload` | `operator` | yes |
| `browser.view.scroll` | `operator` | yes |
| `browser.view.subscribe` | `operator` | yes |
| `browser.view.tab_close` | `operator` | yes |
| `browser.view.tab_open` | `operator` | yes |
| `browser.view.tab_switch` | `operator` | yes |
| `browser.view.take_control` | `operator` | yes |
| `browser.view.type` | `operator` | yes |
| `browser.view.unsubscribe` | `operator` | yes |

### `builder`

| Method | Caller role | Documented |
|---|---|---|
| `builder.build` | `operator` | yes |

### `calendar`

| Method | Caller role | Documented |
|---|---|---|
| `calendar.create_event` | `operator` | yes |
| `calendar.delete_event` | `operator` | yes |
| `calendar.events` | `operator` | yes |
| `calendar.list` | `operator` | yes |
| `calendar.update_event` | `operator` | yes |

### `capabilities`

| Method | Caller role | Documented |
|---|---|---|
| `capabilities.list` | `operator` | yes |

### `cascade`

| Method | Caller role | Documented |
|---|---|---|
| `cascade.run` | `operator` | yes |

### `coder`

| Method | Caller role | Documented |
|---|---|---|
| `coder.approve_merge` | `operator` | yes |
| `coder.cancel` | `operator` | yes |
| `coder.confirm_contract` | `operator` | yes |
| `coder.discuss.close` | `operator` | yes |
| `coder.discuss.list` | `operator` | yes |
| `coder.discuss.promote` | `operator` | yes |
| `coder.discuss.send` | `operator` | yes |
| `coder.discuss.start` | `operator` | yes |
| `coder.discuss.subscribe` | `operator` | yes |
| `coder.discuss.unsubscribe` | `operator` | yes |
| `coder.get` | `operator` | yes |
| `coder.list` | `operator` | yes |
| `coder.projects.create` | `operator` | yes |
| `coder.projects.get` | `operator` | yes |
| `coder.projects.list` | `operator` | yes |
| `coder.respond` | `operator` | yes |
| `coder.revise_contract` | `operator` | yes |
| `coder.start` | `operator` | yes |
| `coder.subscribe` | `operator` | yes |
| `coder.unsubscribe` | `operator` | yes |
| `coder.unwatch` | `operator` | yes |
| `coder.watch` | `operator` | yes |

### `concierge`

| Method | Caller role | Documented |
|---|---|---|
| `concierge.actions` | `operator` | yes |
| `concierge.apply` | `operator` | yes |
| `concierge.ask` | `operator` | yes |
| `concierge.clear_default` | `operator` | yes |
| `concierge.defaults` | `operator` | yes |
| `concierge.dismiss` | `operator` | yes |
| `concierge.refresh_catalog` | `operator` | yes |
| `concierge.rollback` | `operator` | yes |
| `concierge.set_default` | `operator` | yes |
| `concierge.status` | `operator` | yes |

### `connectors`

| Method | Caller role | Documented |
|---|---|---|
| `connectors.add` | `operator` | yes |
| `connectors.add_stdio` | `operator` | yes |
| `connectors.authenticate` | `operator` | yes |
| `connectors.complete_authentication` | `operator` | yes |
| `connectors.disable_tools` | `operator` | yes |
| `connectors.enable_tools` | `operator` | yes |
| `connectors.list` | `operator` | yes |
| `connectors.refresh` | `operator` | yes |
| `connectors.remove` | `operator` | yes |
| `connectors.tools` | `operator` | yes |

### `contacts`

| Method | Caller role | Documented |
|---|---|---|
| `contacts.containers` | `operator` | yes |
| `contacts.find` | `operator` | yes |

### `declagents`

| Method | Caller role | Documented |
|---|---|---|
| `declagents.get` | `operator` | yes |
| `declagents.invoke` | `operator` | yes |
| `declagents.list` | `operator` | yes |
| `declagents.remove` | `host` | yes |
| `declagents.route` | `operator` | yes |
| `declagents.route_split` | `operator` | yes |
| `declagents.routing_stats` | `operator` | yes |
| `declagents.set_enabled` | `host` | yes |

### `diagnostics`

| Method | Caller role | Documented |
|---|---|---|
| `diagnostics.secret_store_activity` | `host` | yes |

### `discovery`

| Method | Caller role | Documented |
|---|---|---|
| `discovery.report` | `operator` | yes |
| `discovery.resolve` | `operator` | yes |
| `discovery.route_compose` | `operator` | yes |

### `events`

| Method | Caller role | Documented |
|---|---|---|
| `events.chain.enable` | `operator` | yes |
| `events.chain.verify` | `operator` | yes |
| `events.clear` | `operator` | yes |
| `events.cost_by_agent` | `operator` | yes |
| `events.count` | `operator` | yes |
| `events.query` | `operator` | yes |
| `events.retention` | `operator` | yes |
| `events.stats` | `operator` | yes |
| `events.truncate` | `operator` | yes |

### `evolution`

| Method | Caller role | Documented |
|---|---|---|
| `evolution.plan` | `operator` | yes |
| `evolution.run` | `agent` | yes |

### `feedback`

| Method | Caller role | Documented |
|---|---|---|
| `feedback.compose_preview` | `operator` | yes |
| `feedback.list` | `operator` | yes |
| `feedback.status` | `operator` | yes |
| `feedback.submit` | `operator` | yes |

### `files`

| Method | Caller role | Documented |
|---|---|---|
| `files.locations` | `operator` | yes |

### `fleet`

| Method | Caller role | Documented |
|---|---|---|
| `fleet.composite` | `operator` | yes |
| `fleet.inventory` | `operator` | yes |
| `fleet.worker.get` | `operator` | yes |
| `fleet.worker.set` | `operator` | yes |

### `foreman`

| Method | Caller role | Documented |
|---|---|---|
| `foreman.plan` | `operator` | yes |
| `foreman.run` | `operator` | yes |

### `goal`

| Method | Caller role | Documented |
|---|---|---|
| `goal.clear` | `operator` | yes |
| `goal.set` | `operator` | yes |
| `goal.status` | `operator` | yes |
| `goal.suggest` | `operator` | yes |

### `heal`

| Method | Caller role | Documented |
|---|---|---|
| `heal.run` | `operator` | yes |
| `heal.status` | `operator` | yes |

### `health`

| Method | Caller role | Documented |
|---|---|---|
| `health.activity` | `operator` | yes |
| `health.sleep` | `operator` | yes |
| `health.status` | `operator` | yes |
| `health.workouts` | `operator` | yes |

### `host`

| Method | Caller role | Documented |
|---|---|---|
| `host.agents` | `operator` | yes |
| `host.approvals` | `operator` | yes |
| `host.devices` | `operator` | yes |
| `host.events` | `operator` | yes |
| `host.notify` | `operator` | yes |
| `host.register_agent` | `operator` | yes |
| `host.register_device` | `operator` | yes |
| `host.request_approval` | `agent` | yes |
| `host.resolve_approval` | `agent` | yes |
| `host.set_status` | `operator` | yes |
| `host.subscribe` | `operator` | yes |
| `host.unregister_agent` | `operator` | yes |
| `host.update_device` | `operator` | yes |

### `image`

| Method | Caller role | Documented |
|---|---|---|
| `image.generate` | `operator` | yes |

### `infer`

| Method | Caller role | Documented |
|---|---|---|
| `infer.cancel` | `operator` | yes |
| `infer.deadline` | `operator` | yes |

### `inference`

| Method | Caller role | Documented |
|---|---|---|
| `inference.register_runner` | `operator` | yes |
| `inference.runner.complete` | `operator` | yes |
| `inference.runner.event` | `operator` | yes |
| `inference.runner.fail` | `operator` | yes |

### `keychain`

| Method | Caller role | Documented |
|---|---|---|
| `keychain.status` | `operator` | yes |

### `lease`

| Method | Caller role | Documented |
|---|---|---|
| `lease.acquire` | `agent` | yes |
| `lease.release` | `agent` | yes |
| `lease.renew` | `agent` | yes |
| `lease.status` | `operator` | yes |

### `mail`

| Method | Caller role | Documented |
|---|---|---|
| `mail.accounts` | `operator` | yes |
| `mail.inbox` | `operator` | yes |
| `mail.mailboxes` | `operator` | yes |
| `mail.message_body` | `operator` | yes |
| `mail.messages` | `operator` | yes |
| `mail.send` | `operator` | yes |

### `meeting`

| Method | Caller role | Documented |
|---|---|---|
| `meeting.get` | `operator` | yes |
| `meeting.list` | `operator` | yes |
| `meeting.start` | `operator` | yes |
| `meeting.stop` | `operator` | yes |

### `memory`

| Method | Caller role | Documented |
|---|---|---|
| `memory.add_fact` | `agent` | yes |
| `memory.admission_table` | `operator` | yes |
| `memory.build_context` | `operator` | yes |
| `memory.build_context_fast` | `operator` | yes |
| `memory.consolidate` | `agent` | yes |
| `memory.delete` | `agent` | yes |
| `memory.evaluate` | `operator` | yes |
| `memory.fact_count` | `operator` | yes |
| `memory.intervene` | `operator` | yes |
| `memory.load` | `operator` | yes |
| `memory.maintain` | `agent` | yes |
| `memory.persist` | `operator` | yes |
| `memory.query` | `operator` | yes |
| `memory.save_knowledge` | `agent` | yes |
| `memory.save_procedural` | `agent` | yes |
| `memory.set_admission_table` | `operator` | yes |
| `memory.update_status` | `operator` | yes |
| `memory.utility_get` | `operator` | yes |
| `memory.utility_set` | `operator` | yes |

### `message`

| Method | Caller role | Documented |
|---|---|---|
| `message/send` | `operator` | yes |
| `message/stream` | `operator` | yes |

### `messages`

| Method | Caller role | Documented |
|---|---|---|
| `messages.chats` | `operator` | yes |
| `messages.read` | `operator` | yes |
| `messages.send` | `operator` | yes |
| `messages.services` | `operator` | yes |

### `messaging`

| Method | Caller role | Documented |
|---|---|---|
| `messaging.config.get` | `host` | yes |
| `messaging.config.set` | `host` | yes |
| `messaging.pairing.start` | `host` | yes |
| `messaging.pairing.status` | `host` | yes |
| `messaging.status` | `host` | yes |
| `messaging.test_send` | `host` | yes |

### `metrics`

| Method | Caller role | Documented |
|---|---|---|
| `metrics.alerts` | `operator` | yes |
| `metrics.summary` | `operator` | yes |

### `mobile`

| Method | Caller role | Documented |
|---|---|---|
| `mobile.runtime` | `operator` | yes |

### `models`

| Method | Caller role | Documented |
|---|---|---|
| `models.adopt` | `host` | yes |
| `models.catalog_snapshot` | `operator` | yes |
| `models.check_concierge` | `operator` | yes |
| `models.check_upgrade_nudge` | `operator` | yes |
| `models.detect_upgrades` | `operator` | yes |
| `models.dismiss_suggestion` | `operator` | yes |
| `models.dismiss_upgrade` | `operator` | yes |
| `models.install` | `host` | yes |
| `models.list` | `operator` | yes |
| `models.list_unified` | `operator` | yes |
| `models.preflight` | `operator` | yes |
| `models.pull` | `host` | yes |
| `models.recommend` | `operator` | yes |
| `models.register` | `operator` | yes |
| `models.remove` | `host` | yes |
| `models.resource_policy.get` | `operator` | yes |
| `models.resource_policy.set` | `host` | yes |
| `models.route` | `operator` | yes |
| `models.route_provenance` | `operator` | yes |
| `models.search` | `operator` | yes |
| `models.setup_plan` | `operator` | yes |
| `models.stats` | `operator` | yes |
| `models.storage_roots` | `host` | yes |
| `models.unregister` | `operator` | yes |
| `models.update_prefs_get` | `operator` | yes |
| `models.update_prefs_set` | `operator` | yes |
| `models.upgrades` | `operator` | yes |

### `multi`

| Method | Caller role | Documented |
|---|---|---|
| `multi.map_reduce` | `operator` | yes |
| `multi.pipeline` | `operator` | yes |
| `multi.subtask` | `operator` | yes |
| `multi.supervisor` | `operator` | yes |
| `multi.swarm` | `operator` | yes |
| `multi.tournament` | `operator` | yes |
| `multi.vote` | `operator` | yes |

### `nlp`

| Method | Caller role | Documented |
|---|---|---|
| `nlp.extract_entities` | `operator` | yes |
| `nlp.identify_language` | `operator` | yes |
| `nlp.tokenize` | `operator` | yes |

### `notes`

| Method | Caller role | Documented |
|---|---|---|
| `notes.accounts` | `operator` | yes |
| `notes.find` | `operator` | yes |

### `notifications`

| Method | Caller role | Documented |
|---|---|---|
| `notifications.local` | `operator` | yes |

### `openrouter`

| Method | Caller role | Documented |
|---|---|---|
| `openrouter.auth_cancel` | `host` | yes |
| `openrouter.auth_start` | `host` | yes |
| `openrouter.disconnect` | `host` | yes |
| `openrouter.status` | `host` | yes |

### `outcomes`

| Method | Caller role | Documented |
|---|---|---|
| `outcomes.resolve_pending` | `operator` | yes |
| `outcomes.scoreboard` | `operator` | yes |

### `parslee`

| Method | Caller role | Documented |
|---|---|---|
| `parslee.auth` | `operator` | yes |
| `parslee.capabilities` | `operator` | yes |
| `parslee.m365.generate_document` | `operator` | yes |

### `permission`

| Method | Caller role | Documented |
|---|---|---|
| `permission.approve` | `host` | yes |
| `permission.classify` | `operator` | yes |
| `permission.evaluate` | `agent` | yes |
| `permission.get_tier` | `operator` | yes |
| `permission.pending` | `agent` | yes |
| `permission.reject` | `host` | yes |
| `permission.set_tier` | `host` | yes |

### `permissions`

| Method | Caller role | Documented |
|---|---|---|
| `permissions.domains` | `operator` | yes |
| `permissions.explain` | `operator` | yes |
| `permissions.request` | `operator` | yes |
| `permissions.status` | `operator` | yes |

### `photos`

| Method | Caller role | Documented |
|---|---|---|
| `photos.albums` | `operator` | yes |

### `policy`

| Method | Caller role | Documented |
|---|---|---|
| `policy.list` | `operator` | yes |
| `policy.register` | `operator` | yes |
| `policy.unregister` | `operator` | yes |

### `proposal`

| Method | Caller role | Documented |
|---|---|---|
| `proposal.submit` | `owner` | yes |

### `registry`

| Method | Caller role | Documented |
|---|---|---|
| `registry.heartbeat` | `operator` | yes |
| `registry.list` | `operator` | yes |
| `registry.reap` | `operator` | yes |
| `registry.register` | `operator` | yes |
| `registry.unregister` | `operator` | yes |

### `reminders`

| Method | Caller role | Documented |
|---|---|---|
| `reminders.items` | `operator` | yes |
| `reminders.lists` | `operator` | yes |

### `replan`

| Method | Caller role | Documented |
|---|---|---|
| `replan.set_config` | `operator` | yes |

### `root`

| Method | Caller role | Documented |
|---|---|---|
| `CancelTask` | `operator` | yes |
| `CreateTaskPushNotificationConfig` | `operator` | yes |
| `DeleteTaskPushNotificationConfig` | `operator` | yes |
| `GetExtendedAgentCard` | `operator` | yes |
| `GetTask` | `operator` | yes |
| `GetTaskPushNotificationConfig` | `operator` | yes |
| `ListTaskPushNotificationConfigs` | `operator` | yes |
| `ListTasks` | `operator` | yes |
| `SendMessage` | `operator` | yes |
| `SendStreamingMessage` | `operator` | yes |
| `SubscribeToTask` | `operator` | yes |
| `classify` | `operator` | yes |
| `detokenize` | `operator` | yes |
| `embed` | `operator` | yes |
| `infer` | `agent` | yes |
| `infer_stream` | `agent` | yes |
| `rerank` | `operator` | yes |
| `search` | `operator` | yes |
| `synthesize` | `operator` | yes |
| `tokenize` | `operator` | yes |
| `transcribe` | `operator` | yes |
| `verify` | `operator` | yes |
| `web_fetch` | `operator` | yes |

### `runs`

| Method | Caller role | Documented |
|---|---|---|
| `runs.cancel` | `owner` | yes |
| `runs.complete` | `owner` | yes |
| `runs.get_trace` | `owner` | yes |
| `runs.list` | `owner` | yes |
| `runs.record_turns` | `agent` | yes |
| `runs.resume` | `agent` | yes |
| `runs.start` | `agent` | yes |
| `runs.subscribe` | `owner` | yes |
| `runs.unsubscribe` | `operator` | yes |

### `scheduler`

| Method | Caller role | Documented |
|---|---|---|
| `scheduler.create` | `operator` | yes |
| `scheduler.os_install` | `operator` | yes |
| `scheduler.os_list` | `operator` | yes |
| `scheduler.os_reconcile` | `operator` | yes |
| `scheduler.os_render` | `operator` | yes |
| `scheduler.os_uninstall` | `operator` | yes |
| `scheduler.run` | `operator` | yes |
| `scheduler.run_loop` | `operator` | yes |

### `secret`

| Method | Caller role | Documented |
|---|---|---|
| `secret.available` | `operator` | yes |
| `secret.delete` | `operator` | yes |
| `secret.get` | `agent` | yes |
| `secret.list` | `operator` | yes |
| `secret.put` | `operator` | yes |
| `secret.status` | `operator` | yes |

### `selfheal`

| Method | Caller role | Documented |
|---|---|---|
| `selfheal.detections` | `operator` | yes |
| `selfheal.dismiss` | `operator` | yes |
| `selfheal.fix` | `operator` | yes |
| `selfheal.run` | `operator` | yes |
| `selfheal.status` | `operator` | yes |

### `session`

| Method | Caller role | Documented |
|---|---|---|
| `session.auth` | `agent` | yes |
| `session.bindSandbox` | `agent` | yes |
| `session.bindSubstrate` | `operator` | yes |
| `session.clear_halt` | `host` | yes |
| `session.init` | `operator` | yes |
| `session.policy.close` | `operator` | yes |
| `session.policy.open` | `operator` | yes |

### `skill`

| Method | Caller role | Documented |
|---|---|---|
| `skill.adopt_pack` | `operator` | yes |
| `skill.enforce_deployment` | `operator` | yes |
| `skill.export` | `operator` | yes |
| `skill.find` | `operator` | yes |
| `skill.gate_deployment` | `operator` | yes |
| `skill.import` | `operator` | yes |
| `skill.ingest` | `operator` | yes |
| `skill.ingest_governed` | `operator` | yes |
| `skill.meta` | `operator` | yes |
| `skill.repair` | `operator` | yes |
| `skill.report` | `operator` | yes |

### `skills`

| Method | Caller role | Documented |
|---|---|---|
| `skills.distill` | `operator` | yes |
| `skills.domains_needing_evolution` | `operator` | yes |
| `skills.evolve` | `operator` | yes |
| `skills.gate` | `operator` | yes |
| `skills.ingest_distilled` | `operator` | yes |
| `skills.ingest_provisional` | `operator` | yes |
| `skills.list` | `operator` | yes |

### `speech`

| Method | Caller role | Documented |
|---|---|---|
| `speech.prepare` | `operator` | yes |

### `state`

| Method | Caller role | Documented |
|---|---|---|
| `state.exists` | `operator` | yes |
| `state.get` | `operator` | yes |
| `state.keys` | `operator` | yes |
| `state.set` | `operator` | yes |
| `state.snapshot` | `operator` | yes |

### `supervision`

| Method | Caller role | Documented |
|---|---|---|
| `supervision.decide` | `operator` | yes |
| `supervision.pending` | `operator` | yes |
| `supervision.subscribe` | `operator` | yes |
| `supervision.unsubscribe` | `operator` | yes |

### `sync`

| Method | Caller role | Documented |
|---|---|---|
| `sync.append` | `operator` | yes |
| `sync.assistant_action.get` | `operator` | yes |
| `sync.assistant_action.put` | `operator` | yes |
| `sync.assistant_checkpoint.get` | `operator` | yes |
| `sync.assistant_checkpoint.put` | `operator` | yes |
| `sync.checkpoint` | `operator` | yes |
| `sync.fence_check` | `agent` | yes |
| `sync.knowledge` | `operator` | yes |
| `sync.pump` | `operator` | yes |
| `sync.rebase` | `operator` | yes |
| `sync.record_intent` | `agent` | yes |
| `sync.record_turn` | `operator` | yes |
| `sync.resume` | `operator` | yes |
| `sync.status` | `operator` | yes |
| `sync.transcript` | `operator` | yes |

### `tasks`

| Method | Caller role | Documented |
|---|---|---|
| `tasks.list` | `operator` | yes |
| `tasks.schedule` | `host` | yes |
| `tasks.unschedule` | `host` | yes |
| `tasks/cancel` | `operator` | yes |
| `tasks/get` | `operator` | yes |
| `tasks/list` | `operator` | yes |
| `tasks/pushNotificationConfig/delete` | `operator` | yes |
| `tasks/pushNotificationConfig/get` | `operator` | yes |
| `tasks/pushNotificationConfig/list` | `operator` | yes |
| `tasks/pushNotificationConfig/set` | `operator` | yes |
| `tasks/resubscribe` | `operator` | yes |

### `tools`

| Method | Caller role | Documented |
|---|---|---|
| `tools.cancel` | `operator` | yes |
| `tools.list` | `operator` | yes |
| `tools.poll` | `operator` | yes |
| `tools.register` | `operator` | yes |
| `tools.stream.subscribe` | `operator` | yes |
| `tools.unregister` | `operator` | yes |

### `verify`

| Method | Caller role | Documented |
|---|---|---|
| `verify.monte_carlo` | `operator` | yes |

### `video`

| Method | Caller role | Documented |
|---|---|---|
| `video.generate` | `operator` | yes |

### `vision`

| Method | Caller role | Documented |
|---|---|---|
| `vision.ocr` | `operator` | yes |

### `voice`

| Method | Caller role | Documented |
|---|---|---|
| `voice.cancel_turn` | `operator` | yes |
| `voice.dispatch_turn` | `operator` | yes |
| `voice.enroll_speaker` | `operator` | yes |
| `voice.list_enrollments` | `operator` | yes |
| `voice.prepare_diarizer` | `operator` | yes |
| `voice.prepare_parakeet` | `operator` | yes |
| `voice.prewarm_turn` | `operator` | yes |
| `voice.providers.list` | `operator` | yes |
| `voice.remove_enrollment` | `operator` | yes |
| `voice.sessions.list` | `operator` | yes |
| `voice.transcribe_stream.push` | `operator` | yes |
| `voice.transcribe_stream.start` | `operator` | yes |
| `voice.transcribe_stream.stop` | `operator` | yes |
| `voice.tts_stream.cancel` | `operator` | yes |
| `voice.tts_stream.list` | `operator` | yes |
| `voice.tts_stream.start` | `operator` | yes |

### `workflow`

| Method | Caller role | Documented |
|---|---|---|
| `workflow.build_automation` | `operator` | yes |
| `workflow.chain` | `operator` | yes |
| `workflow.list_paused` | `operator` | yes |
| `workflow.resume` | `operator` | yes |
| `workflow.run` | `operator` | yes |
| `workflow.verify` | `operator` | yes |

{% endraw %}
